[project]
name = "fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abhishek", email = "abhishek.debnath12nov@outlook.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fastapi = "fastapi:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
